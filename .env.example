
# Environment
NODE_ENV=development

# Database - MSSQL
DB_HOST=192.168.10.76
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=atiya@999
DB_NAME=DristhiSoftTechDBOld
DB_DIALECT=mssql

# JWT Configuration
JWT_SECRET=supersecret
JWT_EXPIRY=7d

# Server Configuration
PORT=5000
APP_URL=http://localhost:5000
DB_SYNC=false

# Optional: password hash rounds for bcrypt (defaults to 10 if not set)
# PASSWORD_HASH_ROUNDS=10

# Logging
LOG_LEVEL=debug

# CORS
CORS_ORIGIN=*

# Sentry Error Tracking (Optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_ENVIRONMENT=development
# SENTRY_TRACES_SAMPLE_RATE=1.0

# Prometheus Metrics (Optional)
# PROMETHEUS_ENABLED=true
# PROMETHEUS_PORT=9090

# ---------- Production notes ----------
# When deploying to production, DO NOT store secrets in this file.
# Use environment variables provided by your host (Kubernetes Secrets, Docker
# secrets, or GitHub Actions/CI secrets). Example values below are placeholders
# to show required keys.
# DB_HOST=db.example.com
# DB_USER=prod_db_user
# DB_PASSWORD=ChangeMeToASecureValue
# DB_NAME=ameyo_prod_db
# JWT_SECRET=replace_with_strong_secret_from_vault
# SA_PASSWORD=ChangeMeToASecureValue  # only used if running SQL Server container
# SENTRY_DSN=https://your-production-sentry-dsn@sentry.io/project-id
# SENTRY_ENVIRONMENT=production
